services:
  pivot-target:
    image: alpine:3.19
    networks:
      pivot_net:
        ipv4_address: 10.99.1.10
    command: ["sleep", "infinity"]

networks:
  pivot_net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.99.1.0/24
