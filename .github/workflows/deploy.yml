name: Deploy SentinelZero

on:
  workflow_dispatch:

env:
  NODE_VERSION: '22'
  PYTHON_VERSION: '3.12'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy placeholder
        run: |
          echo "Deploy is manual-only. CI runs in ci.yml."
          echo "Configure deploy secrets and scripts before enabling remote deploy."
